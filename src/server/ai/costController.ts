export class CostController { private readonly requests = new Map<string, { day: string; count: number }>(); constructor(private readonly dailyLimit: number) {} allow(userId: string) { const day = new Date().toISOString().slice(0, 10); const current = this.requests.get(userId); const count = current?.day === day ? current.count : 0; if (count >= this.dailyLimit) return false; this.requests.set(userId, { day, count: count + 1 }); return true; } }
